version: "3.9"

services:
  langgraph-agent-api:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    env_file:
      - ../.env
    ports:
      - "8000:8000"
    restart: unless-stopped
